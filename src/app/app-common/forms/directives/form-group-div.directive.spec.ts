import { FormGroupDivDirective } from './form-group-div.directive';

describe('FormGroupDivDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupDivDirective();
    expect(directive).toBeTruthy();
  });
});
